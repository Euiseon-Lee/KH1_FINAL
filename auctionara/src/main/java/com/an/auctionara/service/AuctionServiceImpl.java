package com.an.auctionara.service;

import java.io.IOException;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import com.an.auctionara.entity.AuctionDto;
import com.an.auctionara.repository.AttachmentDao;
import com.an.auctionara.repository.AuctionDao;

@Service
public class AuctionServiceImpl implements AuctionService {
	/*
	 * @Autowired private AuctionDao auctionDao;
	 * 
	 * @Transactional
	 * 
	 * @Override public void insert(AuctionDto auctionDto) {
	 * auctionDao.insert(auctionDto); photoDao.insert(auctionDto.getAuctionNo()); }
	 */
}
